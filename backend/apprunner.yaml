version: 1.0
runtime: nodejs22
build:
  commands:
    build:
      - npm install --production
run:
  command: node server.js
  network:
    port: 5000
    env: PORT
  env:
    - name: NODE_ENV
      value: production
